# Example Rules

- Examples should be meaningful end-to-end entry points, not isolated engine invocations unless the directory is explicitly a low-level engine utility.
- If an example changes inputs, outputs, model artifacts, or build steps, update the README and sample invocation together.
- Example build commands should align with the runtime command they demonstrate.
- Do not add examples that require private artifacts unless they are excluded from release staging.
